import { describe, expect, test } from "@jest/globals";

describe("example test", () => {
  test("used as a placeholder to test the CI until we have proper tests", () => {
    expect(true).toBe(true);
  });
});
